module IPrime;

interface IPrime {
	Unit divide (Int n);
}

interface ISieve {
	Unit sieve();
}

class Sieve(Int bound) {
	Int n = 0;
	IPrime two;
	{
		two = new Prime(2);
		n = 3;
	}
	Unit run() {
		while (n < bound) {
			two!divide(n) ;
		}
	}
}

class Prime (Int p) implements IPrime {

}

